# Implementation Matrix

| Capability                    | Current Evidence                                                                    | Status                                               | Implementation Target                                                                          |
| ----------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Gamma market discovery        | `server/agent/polymarket-client.ts`, `server/agent/market-scanner.ts`               | Implemented with tests                               | Preserve and verify                                                                            |
| CLOB orderbook normalization  | `server/agent/polymarket-client.ts`, `server/agent/market-scanner.ts`               | Implemented with tests                               | Preserve and verify                                                                            |
| Market rejection filters      | `server/agent/market-scanner.ts`, `server/agent/risk-manager.ts`                    | Implemented with tests                               | Preserve and verify                                                                            |
| Portfolio reconciliation gate | `server/agent/reconciliation.ts`, orchestrator reconciliation check                 | Implemented core logic                               | Preserve and verify                                                                            |
| Paper execution               | `server/agent/paper-execution.ts`                                                   | Implemented with tests                               | Preserve and verify                                                                            |
| Polymarket live execution     | `server/exchange/polymarket/*`, `server/execution.ts`                               | Implemented fail-closed with injected-client tests   | Requires configured private key, SDK, L2 credentials, allowances, and kill switch              |
| Live readiness configuration  | `.env.example`, `getPolymarketLiveReadiness`, `bot.setExecutionMode`                | Implemented                                          | Blocks live mode and reports missing fields until wallet/funder/RPC/kill switch are configured |
| L2 credential cache           | `server/exchange/polymarket/credentials.ts`                                         | Implemented with AES-256-GCM encrypted disk cache    | Uses SDK `createOrDeriveApiKey()` for `/auth/api-key` and `/auth/derive-api-key`               |
| Live order lifecycle          | `server/exchange/polymarket/orders.ts`, `reconciler.ts`, `positions.ts`, `index.ts` | Implemented place, cancel, sync, REST reconciliation | Websocket user channel optional; REST poll fallback remains available                          |
| Decision audits               | `server/agent/audit-persistence.ts`, `decisionAudits` schema                        | Implemented and verified                             | Persist risk, selection, and deep-edge diagnostics                                             |
| Opportunity selection         | `server/agent/market-selection.ts`                                                  | Implemented and verified                             | Scores edge, confidence, liquidity, and time remaining                                         |
| Intelligence engine           | `NoopIntelligenceEngine`, `RuleBasedIntelligenceEngine`, `LLMIntelligenceEngine`    | Implemented fail-closed                              | Live use requires configured model backend                                                     |
| Anomaly scanner               | `server/intelligence/anomaly-scanner.ts`                                            | Implemented and verified                             | Scores cross-market, temporal, divergence, and whale-pressure anomalies                        |
| Deep reasoner                 | `server/intelligence/deep-reasoner.ts`                                              | Implemented and verified through injectable provider | Ollama provider is fail-closed when unavailable                                                |
| Deep edge gate                | `server/agent/deep-edge-gate.ts`                                                    | Implemented and wired into orchestrator              | Blocks execution unless anomaly score, confidence, and correction thresholds pass              |
| Vector memory retrieval       | `server/memory/vector-retrieval.ts`                                                 | Implemented and verified                             | Structural embeddings and top-k cosine retrieval through a store interface                     |
| Basket arbitrage builder      | `server/strategy/basket-builder.ts`                                                 | Implemented and verified                             | Bregman projection and zero-risk positive-EV validation                                        |
| Educated edge metrics         | `server/monitoring/educated-edge-metrics.ts`                                        | Implemented and verified                             | Tracks invisible-edge ratio, hidden-edge hit rate, and P&L                                     |
| Agent tRPC API                | `server/agent-router.ts`                                                            | Implemented for scan and audits                      | Verify                                                                                         |
| Prototype bot router          | `server/bot-routers.ts`                                                             | Returns persisted bot state                          | Non-production control surface; avoid false dashboard claims                                   |
| Legacy prototype loop         | `server/bot-engine.ts`, `server/ingestion.ts`, `server/execution.ts`                | Hardened where reachable                             | Market ingestion delegates to agent scanner; live execution delegates to adapter               |
| Frontend dashboard            | `client/src/pages/Dashboard.tsx`                                                    | Bound to prototype bot data                          | Non-production                                                                                 |
| Proof reports                 | Required docs in repo root                                                          | Created and maintained                               | Update after gates run                                                                         |
